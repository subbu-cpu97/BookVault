
namespace BookVault.Domain.Events;

// Marker interface — no members, just marks a class as a domain event
// MediatR's INotification lets us publish these through the pipeline
public interface IDomainEvent
{
    
}